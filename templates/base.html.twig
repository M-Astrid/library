<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Library{% endblock %}</title>
        {% block stylesheets %}
            {# the CSS file lives at "public/css/custom.css" #}
            <link href="{{ asset('css/custom.css') }}" rel="stylesheet"/>
            {# the CSS file lives at "public/css/bootstrap.min.css" #}
            <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet"/>
        {% endblock %}
    </head>
    <body>
    <div class="bg">
        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
